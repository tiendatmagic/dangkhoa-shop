<IfModule mod_rewrite.c>
    RewriteEngine On

    # Bỏ qua thư mục storage
    RewriteCond %{REQUEST_URI} !^/storage

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
